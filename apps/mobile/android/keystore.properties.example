# Copy this file to keystore.properties. The copied file is ignored by Git.
# Keep the upload keystore outside the repository and use an absolute path.
storeFile=/absolute/path/to/cyclepair-upload.jks
storePassword=replace-with-local-secret
keyAlias=replace-with-upload-key-alias
keyPassword=replace-with-local-secret
